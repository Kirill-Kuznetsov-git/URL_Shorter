version: "3.9"

services:

  go:
    container_name: go
    build: .
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - pg
    links:
      - pg
      - redis

  pg:
    container_name: pg
    image: postgres
    restart: always
    env_file:
      - .env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ./data/pg:/var/lib/postgresql/data
    depends_on:
      - redis

  redis:
    container_name: redis
    image: redis
    restart: always
    env_file:
      - .env
    volumes:
      - ./data/redis:/data